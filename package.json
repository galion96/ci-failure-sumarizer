{
  "name": "ci-failure-summarizer",
  "version": "1.0.0",
  "description": "GitHub Action that uses AI to analyze CI/CD failures and sends summaries to Slack",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "node --test src/*.test.js"
  },
  "keywords": [
    "github-action",
    "ci-cd",
    "slack",
    "ai",
    "devops",
    "claude"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@anthropic-ai/sdk": "^0.52.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
